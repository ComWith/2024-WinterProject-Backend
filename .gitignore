# Python이 컴파일하는 캐시 파일과 임시 파일을 무시
*.pyc     # Python 컴파일된 파일
*.pyo     # Python 최적화된 컴파일 파일
__pycache__/  # Python 캐시 디렉토리

# 가상 환경 디렉토리를 무시
venv/

# 환경 변수 파일을 무시 (보안상 중요한 데이터를 포함)
.env      # 환경 변수 파일 (API 키, 데이터베이스 비밀번호 등 포함 가능)

# SQLite 데이터베이스 파일을 무시 (로컬 데이터베이스)
*.sqlite3 # SQLite 데이터베이스 파일

# IDE(통합 개발 환경) 관련 파일을 무시
.idea/    # PyCharm의 프로젝트 설정 디렉토리
.vscode/  # Visual Studio Code의 프로젝트 설정 디렉토리

# 운영 체제에서 생성되는 불필요한 파일을 무시
.DS_Store # macOS에서 자동 생성되는 파일
Thumbs.db # Windows에서 자동 생성되는 파일

# Python 패키징 관련 파일을 무시
*.egg-info/ # Python 패키지 정보 디렉토리
*.egg       # Python 패키지 파일
dist/       # 배포 파일이 저장되는 디렉토리
build/      # 빌드 과정에서 생성된 파일 디렉토리